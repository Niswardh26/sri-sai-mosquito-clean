spring:
  application:
    name: spring-boot-app

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  
  datasource:
    url: jdbc:mysql://sql12.freesqldatabase.com:3306/sql12825992
    username: sql12825992
    password: Sg2mR6is62
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  sql:
    init:
      mode: never
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
        format_sql: true
  
  jackson:
    serialization:
      write-dates-as-timestamps: false
      indent-output: true

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    com.example.app: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

jwt:
  secret: U1JpU2FpMTIzIT8kJV4mKigpXytbXX5AZDhHcUxoZkcwV1o=
  expiration: 86400000

app:
  name: Spring Boot Application
  version: 1.0.0
  upload:
    dir: uploads
