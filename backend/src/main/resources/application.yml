spring:
  application:
    name: spring-boot-app

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  
  datasource:
    url: ${DB_URL:jdbc:mysql://sql12.freesqldatabase.com:3306/sql12825992}
    username: ${DB_USERNAME:sql12825992}
    password: ${DB_PASSWORD:Sg2mR6is62}
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  sql:
    init:
      mode: never
      encoding: UTF-8
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
        format_sql: true
  
  jackson:
    serialization:
      write-dates-as-timestamps: false
      indent-output: true

server:
  port: ${PORT:8080}
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    com.example.app: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

jwt:
  secret: U1JpU2FpMTIzIT8kJV4mKigpXytbXX5AZDhHcUxoZkcwV1o=
  expiration: 86400000

app:
  name: Spring Boot Application
  version: 1.0.0
  upload:
    dir: uploads
