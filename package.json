{
  "name": "sri-sai-mosquito-enterprises",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run start:backend & npm run start:frontend",
    "start:backend": "cd backend && mvn spring-boot:run",
    "start:frontend": "cd frontend && npm start",
    "build": "cd backend && mvn clean package && cd ../frontend && npm run build"
  },
  "engines": {
    "node": ">=14"
  }
}